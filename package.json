{
  "private": "true",
  "scripts": {
    "plain-install": "npm i --cache=.cache/npm --no-optional",
    "build": "antora antora-playbook.yml --stacktrace --fetch --clean",
    "postbuild": "copyfiles auth0_handler.js build/_/js/",
    "preview": "python3 -m http.server --directory build && ngrok http --host-header=rewrite --subdomain=thoughtspotdocs localhost:8000"
  },
  "devDependencies": {
    "antora": "^3.1.10"
  },
  "dependencies": {
    "@asciidoctor/tabs": "^1.0.0-beta.6",
    "copyfiles": "^2.4.1",
    "particles.js": "^2.0.0"
  }
}
